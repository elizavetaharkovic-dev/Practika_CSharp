<Window x:Class="RentCars.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Чат" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="MessagesList" Grid.Row="0" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox x:Name="MessageBox" Width="350" Margin="0,0,10,0"/>
            <Button x:Name="SendButton" Content="Отправить" Width="80" Click="SendButton_Click"/>
        </StackPanel>
    </Grid>
</Window>